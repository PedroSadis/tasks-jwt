<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <% if (error) { %>
            <p class="error"><%= error %>, tente novamente</p>
        <% } %>
        <% if (success) { %>
            <p class="success">Registrado Com Sucesso! Por Favor, Faça Login.</p>
        <% } %>
        <form action="/user/login" method="POST">
            <input type="text" name="username" placeholder="nome de usuario" required>
            <input type="password" name="password" placeholder="Senha" required>
            <button type="submit">Login</button>
        </form>
        <p>Nao tem uma conta? <a href="/register">Registre-se aqui</a></p>
    </div>
</body>
</html>